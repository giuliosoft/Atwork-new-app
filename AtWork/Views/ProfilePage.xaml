<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ffImageLoading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:PancakeControl="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             xmlns:Multilingual="clr-namespace:AtWork.Multilingual"
             xmlns:ffTransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" prism:ViewModelLocator.AutowireViewModel="True"
             ios:Page.UseSafeArea="true" Padding="{StaticResource SafeAreaPadding}"
             NavigationPage.HasNavigationBar="false"
             xmlns:Customcontrol="clr-namespace:AtWork.CustomControls"
             ControlTemplate="{StaticResource NewsDetail_HeaderTemplate}"
             BackgroundColor="{DynamicResource HeaderBackgroundColor}"
             x:Class="AtWork.Views.ProfilePage">
    <ContentPage.Content>
        <StackLayout BackgroundColor="{DynamicResource OffWhiteColor}">
            <ScrollView>
                <StackLayout>
                    <ffImageLoading:CachedImage CacheType="None" Margin="0,35,0,0" Grid.Column="0" Source="{Binding volunteers.UserProfileImage}" LoadingPlaceholder="loading" ErrorPlaceholder="noimage" HeightRequest="{DynamicResource HeightWidth130}" WidthRequest="{DynamicResource HeightWidth130}" HorizontalOptions="CenterAndExpand" VerticalOptions="Start">
                        <ffImageLoading:CachedImage.Transformations>
                            <ffTransformations:CircleTransformation BorderHexColor="#000000" BorderSize="10"/>
                        </ffImageLoading:CachedImage.Transformations>
                    </ffImageLoading:CachedImage>
                    <Label Margin="0,10,0,0" Text="{Binding volunteers.FullName}" Style="{StaticResource PastActivityHeaderLabelStyle}"/>
                    <Label Margin="0,25,0,0" Text="{x:Static Multilingual:AppResources.txtParticipation}" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Center"/>
                    <Grid ColumnDefinitions="*,*" Margin="20,0,20,0">
                        <StackLayout Grid.Column="0" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                            <PancakeControl:PancakeView Style="{StaticResource ProfilePancakeStyle}">
                                <Label Text="{Binding volunteers.Vortex_Activity_Count}" Style="{StaticResource ActivitiesCountLabelStyle}"/>
                                <PancakeControl:PancakeView.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ShowActivityCommand}" CommandParameter="{Binding .}" />
                                </PancakeControl:PancakeView.GestureRecognizers>
                            </PancakeControl:PancakeView>
                            <Label Text="{x:Static Multilingual:AppResources.ActivitiesText}" Style="{StaticResource AboutMeDetailLabelStyle}"/>
                        </StackLayout>
                        <StackLayout Grid.Column="1">
                            <PancakeControl:PancakeView Style="{StaticResource ProfilePancakeStyle}">
                                <Label Text="{Binding volunteers.volHours}" Style="{StaticResource ActivitiesHoursLabelStyle}"/>
                                <PancakeControl:PancakeView.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ShowHoursCommand}" CommandParameter="{Binding .}" />
                                </PancakeControl:PancakeView.GestureRecognizers>
                            </PancakeControl:PancakeView>
                            <Label Text="{x:Static Multilingual:AppResources.HoursText}" Style="{StaticResource AboutMeDetailLabelStyle}"/>
                        </StackLayout>
                    </Grid>
                    <FlexLayout Padding="{DynamicResource MarginPadding00_20_00_20}" IsVisible="{Binding UserActivityHoursList}" BindableLayout.ItemsSource="{Binding UserActivityHoursList}" Wrap="Wrap" Direction="Row" JustifyContent="Center" AlignItems="Start" AlignContent="Start">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <StackLayout IsEnabled="False" Orientation="Vertical" >
                                    <!--<Button Text="14" HeightRequest="50" WidthRequest="50" CornerRadius="25" TextColor="White" BackgroundColor="{StaticResource DarkColor}" HorizontalOptions="Center"></Button>-->
                                    <PancakeControl:PancakeView Style="{StaticResource ProfilePancakeStyle}" HeightRequest="{DynamicResource HeightWidth60}" WidthRequest="{DynamicResource HeightWidth60}" CornerRadius="{DynamicResource CornerRadius30}" BorderThickness="0" BackgroundColor="{StaticResource DarkColor}" >
                                        <Label Text="{Binding Count}" Style="{StaticResource ActivitiesCountLabelStyle}" FontSize="20" TextColor="White"/>
                                    </PancakeControl:PancakeView>
                                    <Label Text="{Binding Text}" WidthRequest="{DynamicResource HeightWidth130}" HorizontalTextAlignment="Center" HorizontalOptions="EndAndExpand" />
                                </StackLayout>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </FlexLayout>
                    <Button Command="{Binding OpenFullActivityCommand}" IsVisible="{Binding isShowSettingLogout}" Margin="{StaticResource MarginPadding20}" Text="{x:Static Multilingual:AppResources.SeeFullActivityHistory}" BackgroundColor="{DynamicResource AccentColor}" HeightRequest="{DynamicResource HeightWidth40}" CornerRadius="{DynamicResource ButtonCornerRadius20}" FontSize="{DynamicResource FontSize16}" FontFamily="Lato_Bold" TextColor="White"/>
                    <BoxView HeightRequest="1" BackgroundColor="Gray"/>
                    <Label Margin="0,25,0,0" Text="{x:Static Multilingual:AppResources.MyInterestsProfile}" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Center"/>
                    <Grid WidthRequest="{DynamicResource HeightWidth320}" ColumnDefinitions="*" Padding="0" HorizontalOptions="Center" VerticalOptions="Center">
                        <FlexLayout Grid.Column="0" x:Name="YourInterestList" BindableLayout.ItemsSource="{Binding interestList}" AlignContent="Center" Direction="Row" Wrap="Wrap" JustifyContent="Center" HorizontalOptions="FillAndExpand" VerticalOptions="Center" Padding="0">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate>
                                    <PancakeControl:PancakeView BorderColor="{StaticResource DarkColor}" BorderThickness="2" Margin="7" HasShadow="False" CornerRadius="15" HeightRequest="30" Padding="{DynamicResource MarginPadding10_05_10_05}" HorizontalOptions="FillAndExpand" VerticalOptions="Center">
                                        <!--<PancakeControl:PancakeView.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" TargetType="PancakeControl:PancakeView" Value="True">
                                                <Setter Property="BackgroundColor" Value="{StaticResource DarkGreenShade}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsSelected}" TargetType="PancakeControl:PancakeView" Value="False">
                                                <Setter Property="BackgroundColor" Value="Transparent" />
                                            </DataTrigger>
                                        </PancakeControl:PancakeView.Triggers>-->
                                        <Label Text="{Binding Title}" Style="{StaticResource FeedbackChipLabelStyle}" TextColor="{StaticResource DarkColor}" LineBreakMode="NoWrap">
                                            <!--<Label.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" TargetType="Label" Value="True">
                                                    <Setter Property="TextColor" Value="{StaticResource WhiteColor}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSelected}" TargetType="Label" Value="False">
                                                    <Setter Property="TextColor" Value="{StaticResource DarkGreenShade}" />
                                                </DataTrigger>
                                            </Label.Triggers>-->
                                        </Label>
                                        <!--<PancakeControl:PancakeView.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={x:Reference YourInterestList}, Path=BindingContext.ActivityFeelingSelectedCommand}" CommandParameter="{Binding .}" />
                                        </PancakeControl:PancakeView.GestureRecognizers>-->
                                    </PancakeControl:PancakeView>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </FlexLayout>
                    </Grid>
                    <Label Margin="0,25,0,0" Text="{x:Static Multilingual:AppResources.AboutMeText}" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Center"/>
                    <Label Margin="50,0,50,20" Text="{Binding volunteers.volAbout}" Style="{StaticResource AboutMeDetailLabelStyle}"/>
                    <BoxView HeightRequest="1" BackgroundColor="Gray"/>
                    <Customcontrol:RepeaterView x:Name="UserGroupList" GridHeightUnitType="Auto" BackgroundColor="Transparent" ItemsSource="{Binding UserGroupList}" ColumnCount="1" Spacing="0" Padding="20" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                        <Customcontrol:RepeaterView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" >
                                    <StackLayout Orientation="Horizontal"  Padding="0,10,5,10" >
                                        <Button IsVisible="{Binding isMyGroup}"  Text="{Binding classDescription}" Style="{StaticResource ProfileGroupButtonStyle}" HeightRequest="{DynamicResource HeightWidth30}" CornerRadius="{DynamicResource ButtonCornerRadius15}"  Padding="{DynamicResource MarginPadding10_00_10_00}"/>
                                        <Label IsVisible="{Binding isGroup}" Text="{Binding classDescription}" Style="{StaticResource ProfileGroupLabelStyle}" Padding="{DynamicResource MarginPadding10_00_00_00}"/>
                                        <Label Text="{Binding classValue}" Style="{StaticResource ProfileHeaderLabelStyle}" TextColor="{DynamicResource AccentColor}" TextDecorations="Underline" HorizontalOptions="EndAndExpand" Padding="0,0,5,0">
                                            <Label.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Source={x:Reference UserGroupList},Path=BindingContext.OpenGroupMemberListCommand}" CommandParameter="{Binding .}"/>
                                            </Label.GestureRecognizers>
                                        </Label>
                                    </StackLayout>
                                </StackLayout>
                            </DataTemplate>
                        </Customcontrol:RepeaterView.ItemTemplate>
                    </Customcontrol:RepeaterView>
                    <BoxView IsVisible="{Binding isShowBoxview}" HeightRequest="1" BackgroundColor="Gray"/>
                    <StackLayout Padding="20" >
                        <!--<Customcontrol:RepeaterView  x:Name="lstUserPersonalDetails" GridHeightUnitType="Auto" BackgroundColor="Transparent" ItemsSource="{Binding UserPersonalDetails}" ColumnCount="1" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                            <Customcontrol:RepeaterView.ItemTemplate>
                                <DataTemplate>
                                    <StackLayout Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                                        <Label Text="{Binding UserDescriptionTitle}" HorizontalTextAlignment="Center" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                                        <Label Text="{Binding UserDescriptionValue}" Style="{StaticResource ProfileHeaderLabelStyle}"/>
                                    </StackLayout>
                                </DataTemplate>
                            </Customcontrol:RepeaterView.ItemTemplate>
                        </Customcontrol:RepeaterView>-->

                        <StackLayout IsVisible="{Binding volunteers.ShowUserBirthdate}"  Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="{x:Static Multilingual:AppResources.BirthDayText}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                            <Label Text="{Binding volunteers.UserBirthdate}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout IsVisible="{Binding volunteers.ShowcoLocation}" Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="{x:Static Multilingual:AppResources.LocationText}"  Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                            <Label Text="{Binding volunteers.coLocation}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout IsVisible="{Binding volunteers.ShowStartDate}" Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="{x:Static Multilingual:AppResources.StartDateText}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                            <Label Text="{Binding volunteers.StartDate}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout IsVisible="{Binding volunteers.ShowEmployeeID}" Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="{x:Static Multilingual:AppResources.EmployeeIdText}" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Start"/>
                            <Label Text="{Binding volunteers.EmployeeID}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout IsVisible="{Binding volunteers.ShowCustomField}" Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="{x:Static Multilingual:AppResources.CustomFieldText}" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Start"/>
                            <Label Text="{Binding volunteers.CustomField}" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout IsVisible="{Binding volunteers.ShowContact}" Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="{x:Static Multilingual:AppResources.ContactMeText}" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Start" VerticalOptions="Start"/>
                            <StackLayout HorizontalOptions="EndAndExpand">
                                <Label Text="{Binding volunteers.volEmail}" Style="{StaticResource ProfileHeaderLabelStyle}" TextDecorations="Underline" TextColor="{DynamicResource AccentColor}" HorizontalOptions="EndAndExpand">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OpenEmailCommand}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                                <Label Text="{Binding volunteers.volPhone}" Style="{StaticResource ProfileHeaderLabelStyle}" TextDecorations="Underline" TextColor="{DynamicResource AccentColor}" HorizontalOptions="EndAndExpand">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OpenCallDialerCommand}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                            </StackLayout>
                        </StackLayout>
                        <!--<StackLayout Padding="0" Spacing="0">
                        <StackLayout Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="BIRTHDAY" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                            <Label Text="November 5" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="LOCATION"  Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                            <Label Text="Zurich" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="START DATE" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="Start"/>
                            <Label Text="September 1 2017" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="EMPLOYEE ID" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Start"/>
                            <Label Text="L00110022" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="CUSTOM FIELD" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Start"/>
                            <Label Text="Entry goed here" Style="{StaticResource ProfileHeaderLabelStyle}" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                        <StackLayout  Orientation="Horizontal" Padding="{StaticResource MarginPadding10}" >
                            <Label Text="CONTACT ME" Style="{StaticResource NewsCommentEditorStyle}" HorizontalOptions="Start" VerticalOptions="Start"/>
                            <StackLayout HorizontalOptions="EndAndExpand">
                                <Label Text="carron.m@atwork.ai" Style="{StaticResource ProfileHeaderLabelStyle}" TextDecorations="Underline" TextColor="{DynamicResource AccentColor}" HorizontalOptions="EndAndExpand">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OpenEmailCommand}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                                <Label Text="+46 780000000" Style="{StaticResource ProfileHeaderLabelStyle}" TextDecorations="Underline" TextColor="{DynamicResource AccentColor}" HorizontalOptions="EndAndExpand">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OpenCallDialerCommand}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                            </StackLayout>
                        </StackLayout>
                            </StackLayout>-->
                    </StackLayout>
                    <StackLayout IsVisible="{Binding isShowSettingLogout}" VerticalOptions="EndAndExpand">
                        <StackLayout>
                            <BoxView HeightRequest="1" BackgroundColor="{DynamicResource AccentColor}"/>
                            <Label Margin="0,7,0,7" Text="{x:Static Multilingual:AppResources.SettingsText}" Style="{StaticResource SettingsLabelStyle}"/>
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SettingCommand}"/>
                            </StackLayout.GestureRecognizers>
                        </StackLayout>
                        <StackLayout>
                            <BoxView HeightRequest="1" BackgroundColor="{DynamicResource AccentColor}"/>
                            <Label Margin="0,7,0,7" Text="{x:Static Multilingual:AppResources.LogoutText}" Style="{StaticResource SettingsLabelStyle}"/>
                            <BoxView HeightRequest="1" BackgroundColor="{DynamicResource AccentColor}"/>
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding LogoutCommand}"/>
                            </StackLayout.GestureRecognizers>
                        </StackLayout>
                    </StackLayout>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
